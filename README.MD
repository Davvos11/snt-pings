# SNT Jinglepings ping toolie

## Usage:
```shell
cargo run --release -- filename.png x y --timeout secs
```
Or:
```shell
cargo build --release
./target/release/snt-pings filename.png x y --timeout secs
```